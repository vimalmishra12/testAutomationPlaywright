{
  "BB": {
    "login": {
      "teacher": {
        "username": "teacher7feb",
        "password": "{{env.BB_THOR_TEACHER_PASSWORD}}"
      },
      "ltiTeacher": {
        "username": "thornodeepltiteacher",
        "password": "{{env.BB_THOR_LTITEACHER_PASSWORD}}"
      },
      "ltiStudent": {
        "username": "thortestltistudent",
        "password": "{{env.BB_THOR_LTISTUDENT_PASSWORD}}"
      },
      "ltiDeeplinkTeacher": {
        "username": "thortestltiteacher",
        "password": "{{env.BB_THOR_LTIDEEPLINKTEACHER_PASSWORD}}"
      }
    }
  }
}
